apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet-boilerplate-api.fullname" . }}
  labels:
    {{- include "dotnet-boilerplate-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dotnet-boilerplate-api.selectorLabels" . | nindent 4 }}
