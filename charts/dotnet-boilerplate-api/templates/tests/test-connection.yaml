apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dotnet-boilerplate-api.fullname" . }}-test-connection"
  labels:
    {{- include "dotnet-boilerplate-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dotnet-boilerplate-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
